name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy 
  - mdtraj
  - json

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - mdshare

    # Pip-only installs
  #- pip:
  #  - codecov

